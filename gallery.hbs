{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

{{> "site-nav"}}

{{!-- The main content area --}}
<div class="content">
        <div class="container">
          <br>
            <main id="site-main" class="site-main outer">
    <div class="inner">
        <div class="post-feed">
{{#get "posts" limit="all" include="authors,tags" filter="tags:gallery"}}
  {{#foreach posts}}
    {{> "post-card"}}
  {{/foreach}}
{{/get}}
        </div>
    </div>
</main>
 </div>
    </div>